// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}


enum Rewards{
  Bounty
}

enum Severity{
  low
  medium 
  high
  critical
}
model Contacts{
  id String @id @default(auto()) @map("_id")@db.ObjectId
  name String
  company_name String
  email String @unique
  contact String 
  message String
  createdAt   DateTime @default(now()) @map("createdAt")
  updatedAt   DateTime @updatedAt @map("updatedAt")

}

model Programs{
  id String @id @default(auto()) @map("_id")@db.ObjectId
  program String
  launched_on DateTime
  rewards Rewards
}

model Reports{
    id String @id @default(auto()) @map("_id")@db.ObjectId
    name String
    email String
    title String
    severity Severity
    type String
    detail String
    steps String[]
    impact String
    affected_system String
    poc Bytes?
    createdAt   DateTime @default(now()) @map("createdAt")
  updatedAt   DateTime @updatedAt @map("updatedAt")
}

model User{
      id String @id @default(auto()) @map("_id")@db.ObjectId
    // name String 
    work_email String
    company_name String
    contact String
    message String
    password String
}
model Validated_User{
      id String @id @default(auto()) @map("_id")@db.ObjectId
    // name String 
    work_email String
    company_name String
    contact String
    message String
    password String
}
// Company Name: [Service Provider Name]
// Logo: [Attach Logo]
// Company Overview : [Provide a brief introduction about the company]
// Services Offered : [List and describe the key cybersecurity services they provide, such as penetration testing, threat intelligence, incident response, compliance consulting, etc.]
// Expertise & Certifications : [Mention relevant certifications (e.g., ISO 27001, CISSP, CEH), partnerships, industry experience, and any notable achievements.]
// Case Studies & Testimonials : [Include success stories, notable clients, or testimonials to establish credibility and showcase past achievements.]
// Website: [Company Website Link]

model Company{
  id String @id @default(auto()) @map("_id")@db.ObjectId
  company_name String
  logo String
  overview String @db.String
  services_offered Json
  expertise_and_certifications Json
  case_studies Json
  website String

}